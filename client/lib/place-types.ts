export interface PlaceOption {
  label: string;
  latitude: number;
  longitude: number;
  address: string;
}

export interface PlaceCenter {
  label: string;
  latitude: number;
  longitude: number;
}
